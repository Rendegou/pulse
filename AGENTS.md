# PULSE 项目协作规则

适用于本仓库内的代码、文档和学习任务。

- Windows 下使用 PowerShell 7（`pwsh`），不使用 `powershell.exe`。
- 首先读取 `docs/README.md` 和 `docs/05-progress-and-validation.md`，再核对工作区；文档进度不能替代源码证据。
- 写代码、改代码、审查时读取 `.agents/skills/comment-convention/SKILL.md`。
- 手写学习、出题、提示、讲解和作业 review 时读取 `.agents/skills/handwrite-coach/SKILL.md`。
- 默认按 `docs/07-ai-assisted-roadmap.md` 使用“AI 完成功能包 + 用户练一个关键点”；功能包与学习练习分别验收，不逐课解锁产品。
- 本规则覆盖陪练 Skill 中“用户负责主要实现”的旧默认；该 Skill 的分级提示、保护手写成果与能力验收继续适用。
- AI 完成当前功能包内的机械工作、接线、测试脚手架与文档；一次只给用户一个约 20–30 分钟的核心练习，给入口、契约、反例、验收和第一级提示。估时不是期限。
- 用户说“提示”“给一点点”时，不替其实现整段功能。明确要求实现或修复时，完成授权范围，并解释关键变化。
- 每个函数/方法及承担业务行为的回调都写中文注释；具体格式以注释 Skill 为准。
- 保留已有手写成果，先看 diff；不要为教学清空函数体、回退工作或替换整文件。
- 进度分别记录“已读懂”“有实现”“有验证”“能独立复写”，不以助手写完代替用户学会。
- 示例或 fixture 必须标明来源，不能作为生产真实流量或真实主机事件显示。
- 只有出现具体职责冲突或测试边界时才拆目录，不预建空的多层架构，不因用户尚未学习拆分就阻塞必要实现。
- 更新协议、行为或代码入口时同步修改对应教程，清理已经失效的 TODO。
- 验证报告区分语法、单元、集成、浏览器和生产环境；没有运行就写未验证。
- 提交、推送和部署按用户本次授权执行；注意 `.github/workflows/deploy.yml` 对 `main` push 自动部署。
